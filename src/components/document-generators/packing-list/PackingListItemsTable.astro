<section class="epl-section epl-items-section items-section">
  <div class="epl-section-head epl-inline-actions epl-items-header items-header">
    <div>
      <h2 class="epl-section-title">Items</h2>
      <p class="epl-section-note">Add item lines below. Totals are calculated automatically.</p>
    </div>

    <button id="addItemButton" type="button" class="epl-add-item-btn add-item-btn">
      <span>+</span>
      Add Item
    </button>
  </div>

  <div class="epl-items-table-shell items-table-shell">
    <table class="epl-items-table items-table">
      <thead>
        <tr>
          <th class="epl-col-description col-description">Item Description</th>
          <th class="epl-col-small col-small">Qty</th>
          <th class="epl-col-small col-small">Unit</th>
          <th class="epl-col-small col-small">Cartons</th>
          <th class="epl-col-medium col-medium">Net Weight</th>
          <th class="epl-col-medium col-medium">Gross Weight</th>
          <th class="epl-col-small col-small">CBM</th>
          <th class="epl-col-action col-action"></th>
        </tr>
      </thead>
      <tbody id="itemsTableBody"></tbody>
    </table>
  </div>

  <div class="epl-totals-box totals-card">
    <div class="epl-totals-grid">
      <div>
        <span>Total Qty</span>
        <strong id="totalQty">0</strong>
      </div>
      <div>
        <span>Total Cartons</span>
        <strong id="totalCartons">0</strong>
      </div>
      <div>
        <span>Total Net Weight</span>
        <strong id="totalNetWeight">0.00</strong>
      </div>
      <div>
        <span>Total Gross Weight</span>
        <strong id="totalGrossWeight">0.00</strong>
      </div>
      <div>
        <span>Total CBM</span>
        <strong id="totalCbm">0.000</strong>
      </div>
    </div>
  </div>
</section>

<style>
  .epl-items-section,
  .items-section {
    margin: 2px 0;
    padding: 0;
    border: 1px solid #e2e8f0;
    border-radius: 24px;
    background: #ffffff;
    overflow: hidden;
    box-shadow: 0 10px 28px rgba(15, 23, 42, 0.04);
  }

  .epl-items-header,
  .items-header {
    align-items: flex-start;
    padding: 26px 28px 22px;
    border-bottom: 1px solid #e5e7eb;
    background: linear-gradient(180deg, #ffffff, #fbfdff);
  }

  .epl-items-header h2,
  .items-header h2 {
    margin: 0;
    font-size: 24px;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: -0.04em;
    color: #0f172a;
  }

  .epl-section-title {
    margin: 0;
    font-size: 0.85rem;
    line-height: 1.3;
    letter-spacing: 0.08em;
    font-weight: 800;
    color: #475569;
    text-transform: uppercase;
  }

  .epl-section-note {
    margin: 6px 0 0;
    font-size: 0.95rem;
    line-height: 1.6;
    color: #64748b;
  }

  .epl-add-item-btn,
  .add-item-btn {
    border: 0;
    border-radius: 999px;
    background: #0f172a;
    color: #ffffff;
    padding: 12px 18px;
    font-size: 14px;
    font-weight: 900;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    white-space: nowrap;
    box-shadow: 0 10px 22px rgba(15, 23, 42, 0.16);
    transition: transform 0.18s ease, background-color 0.18s ease;
  }

  .epl-add-item-btn span,
  .add-item-btn span {
    font-size: 20px;
    line-height: 1;
    margin-top: -1px;
  }

  .epl-add-item-btn:hover,
  .add-item-btn:hover {
    background: #020617;
    transform: translateY(-1px);
  }

  .epl-items-table-shell,
  .items-table-shell {
    padding: 0 20px 20px;
    overflow-x: auto;
    overflow-y: hidden;
    background: #ffffff;
  }

  .epl-items-table,
  .items-table {
    width: 100%;
    min-width: 1080px;
    border-collapse: separate;
    border-spacing: 0 10px;
    table-layout: fixed;
  }

  .epl-items-table thead th,
  .items-table thead th {
    padding: 14px 10px 8px;
    color: #64748b;
    background: transparent;
    text-align: left;
    font-size: 12px;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    white-space: nowrap;
    border: 0;
  }

  .epl-items-table tbody td,
  .items-table tbody td {
    padding: 0 8px;
    border: 0;
    vertical-align: middle;
    background: transparent;
  }

  .epl-items-table tbody tr td:first-child,
  .items-table tbody tr td:first-child {
    padding-left: 0;
  }

  .epl-items-table tbody tr td:last-child,
  .items-table tbody tr td:last-child {
    padding-right: 0;
  }

  .epl-items-table input,
  .items-table input {
    width: 100%;
    height: 46px;
    min-height: 46px;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 10px 12px;
    background: #f8fafc;
    color: #0f172a;
    box-sizing: border-box;
    font-size: 14px;
    outline: none;
    transition:
      background 0.18s ease,
      border-color 0.18s ease,
      box-shadow 0.18s ease;
  }

  .epl-items-table input::placeholder,
  .items-table input::placeholder {
    color: #94a3b8;
  }

  .epl-items-table input:hover,
  .items-table input:hover {
    border-color: #cbd5e1;
    background: #ffffff;
  }

  .epl-items-table input:focus,
  .items-table input:focus {
    border-color: #2563eb;
    background: #ffffff;
    box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.12);
  }

  .epl-items-table .epl-item-desc,
  .epl-items-table .item-desc,
  .items-table .epl-item-desc,
  .items-table .item-desc {
    font-weight: 600;
  }

  .epl-col-description,
  .col-description {
    width: 38%;
  }

  .epl-col-small,
  .col-small {
    width: 8%;
  }

  .epl-col-medium,
  .col-medium {
    width: 12%;
  }

  .epl-col-action,
  .col-action {
    width: 48px;
  }

  .epl-item-remove,
  .remove-item-btn {
    width: 38px;
    height: 38px;
    border: 0;
    border-radius: 999px;
    background: #fff1f2;
    color: #e11d48;
    font-size: 22px;
    font-weight: 800;
    line-height: 1;
    cursor: pointer;
    display: block;
    margin: 0 auto;
    opacity: 0.55;
    transition:
      opacity 0.18s ease,
      background 0.18s ease,
      transform 0.18s ease;
  }

  .epl-items-table tbody tr:hover .epl-item-remove,
  .epl-items-table tbody tr:hover .remove-item-btn,
  .epl-items-table tbody tr.is-focused .epl-item-remove,
  .epl-items-table tbody tr.is-focused .remove-item-btn,
  .items-table tbody tr:hover .epl-item-remove,
  .items-table tbody tr:hover .remove-item-btn,
  .items-table tbody tr.is-focused .epl-item-remove,
  .items-table tbody tr.is-focused .remove-item-btn {
    opacity: 1;
  }

  .epl-item-remove:hover,
  .remove-item-btn:hover {
    background: #ffe4e6;
    transform: scale(1.04);
  }

  .epl-totals-box,
  .totals-card {
    margin: 0 20px 24px;
    border: 1px solid #dbeafe;
    border-radius: 20px;
    background: linear-gradient(180deg, #f8fbff, #ffffff);
    overflow: hidden;
  }

  .epl-totals-grid {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 0;
  }

  .epl-totals-grid div {
    display: flex;
    flex-direction: column;
    gap: 8px;
    padding: 20px 18px;
    border-right: 1px solid #dbe3ef;
  }

  .epl-totals-grid div:last-child {
    border-right: 0;
  }

  .epl-totals-grid span {
    display: block;
    color: #64748b;
    font-size: 12px;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .epl-totals-grid strong {
    display: block;
    font-size: 24px;
    line-height: 1.1;
    letter-spacing: -0.04em;
    color: #0f172a;
    font-weight: 900;
  }

  @media (max-width: 900px) {
    .epl-items-header,
    .items-header {
      flex-direction: column;
      align-items: stretch;
      padding: 22px 20px 18px;
    }

    .epl-add-item-btn,
    .add-item-btn {
      width: 100%;
    }

    .epl-items-table-shell,
    .items-table-shell {
      padding: 0 14px 18px;
    }

    .epl-totals-box,
    .totals-card {
      margin: 0 14px 20px;
    }

    .epl-totals-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .epl-totals-grid div {
      border-right: 0;
      border-bottom: 1px solid #dbe3ef;
    }

    .epl-totals-grid div:last-child {
      border-bottom: 0;
    }
  }

  @media (max-width: 640px) {
    .epl-totals-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
