{
  "name": "worowo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:preview": "wrangler pages dev ./dist",
    "deploy": "wrangler pages deploy ./dist"
  },
  "dependencies": {
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.83.0"
  }
}
